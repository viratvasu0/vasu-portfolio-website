name: SonarQube Analysis Scan
on:
  push:
    branches: [develop, main]
jobs:
  sonarqube-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo Executing Scan...
